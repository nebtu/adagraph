% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adagraph_design.R
\name{adagraph_design}
\alias{adagraph_design}
\title{Make a new (generic) trial design}
\usage{
adagraph_design(
  weights = double(),
  test_m = matrix(),
  alpha = double(),
  correlation = NA,
  names = NULL
)
}
\arguments{
\item{weights}{List of weights, measuring how important each hypothesis is}

\item{test_m}{Transition matrix describing the graph for the closed test procedure to test the hypotheses}

\item{alpha}{Single number, measuring what total alpha should be spent on the FWER}

\item{correlation}{Correlation matrix describing the structure of the correlations
between the different hypotheses, use NA for uncorrelated.
Defaults to no known correlation.}

\item{names}{optional names for the hypotheses}
}
\value{
An object of class adagraph_design
}
\description{
Make a new (generic) trial design
}
\details{
If \code{weights}, \code{test_m}, or \code{correlation} are named (via \code{\link[=names]{names()}} for vectors
or \code{\link[=rownames]{rownames()}}/\code{\link[=colnames]{colnames()}} for matrices), they are automatically reordered
to match the canonical hypothesis order. The canonical order is determined by
the \code{names} argument if provided, otherwise by \code{names(weights)}, otherwise
\code{H1}, \code{H2}, etc. An error is raised if the names do not match the expected
hypothesis names.
}
\examples{
design <- adagraph_design(
 correlation=rbind(H1=c(1, NA),
                   H2=c(NA, 1)),
 weights=c(2/3, 1/3),
 alpha=0.05,
 test_m=rbind(c(0, 1),
              c(1, 0)))
design
}
